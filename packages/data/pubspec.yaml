name: data
description: "A collection of classes, models and repositories to manage the app data."

publish_to: "none"

version: "1.0.0"

environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ^3.29.0

dependencies:
  clock: ^1.1.2
  cloud_firestore: ^5.6.4
  collection: ^1.19.1
  domain:
    path: ../domain
  firebase_auth: ^5.5.0
  flutter:
    sdk: flutter
  http: ^1.3.0
  provider: ^6.1.2
  share_plus: ^10.1.4
  url_launcher: ^6.3.1

dev_dependencies:
  build_runner: ^2.4.15
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.5
